# Base64 Encoder

```
from encoder.base64 import base64_encode

# encode string
string = "hello, world"
encoded = base64_encode(string)

# encode bytes
data = [1, 2, 3]
encoded = base64_encode(data)
```